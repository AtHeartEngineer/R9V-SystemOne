# Transition reliability test: incomplete qualification

The latest attempt ran 3156.57 seconds (52.61 minutes), then correctly failed its independent identity-coverage guard. **The requested full-hour qualification did not pass.** No GPU crash was observed, but missing live recorder coverage prevents a full stability verdict. This is a targeted transition reliability test, not comprehensive worst-case system stress.

It completed 132 normal scenario responses, 42 planned streaming disconnections, and 21 long prefills with actual usage of 130803–130816 prompt tokens. Twenty rounds completed fully; the final round stopped during its idle/resume stage. All completed arithmetic recovery answers were correct. Eleven alternating graphics controls recorded frame progress. The controls are light 512x512 X11 copy workloads, not GPU saturation tests. Server execution remained limited to one sequence, with two concurrent HTTP clients exercising queue/cancellation transitions.

At the failure, the client saw identity age5.191seconds against its5second limit; the separate admission gate also detected missing live identity. Blackbox subsequently showed severe I/O pressure (some avg10=76.66%, full avg10=73.43%). Workstation's original journal retained39 consecutive identity samples across the stop window, maximumgap1.032seconds, all without capture errors. This supports a receiver/recorder-path stall rather than a workstation identity-collector failure; the exact blocking I/O operation has not been isolated. Do not loosen the freshness threshold or count retrospective local records as uninterrupted independent live coverage.

No GPU timeout or allocation warning was captured in this attempt. Minimum recorded physical free VRAM was3.931GiB on GPU03 and3.206GiB on GPU13. Docker exited0, OOMKilled=false; boot unchanged; desktop remained active. The controller retained90.028seconds aftermath. Owned model, graphics, root identity collector, and timeout recorder are stopped. Four unrelated CPU containers and the desktop remain running. No smart-plug recovery was used; cumulative ledger remains5used/1remaining.

Earlier attempts exposed additional limitations:

- 225500: stopped after1353.9seconds on a freshness rejection. Fixed a reproducible clock/read race; original failed ages were not logged, so its exact trigger remains uncertain.
- 233000: stopped after1716.1seconds because a periodic status summary hid fresh heartbeat receipts. A host packet arrived8ms before rejection. The client now uses primary accepted receipt timestamps with unchanged10s host/45s trace limits.
- 001000: admission path/date error, before model startup; corrected in a fresh campaign.
- 235900: real startup normal-zone exhaustion, kernel allocation warning, and nearly full swap. The controller stopped loading, with exit137 from bounded cleanup; no OOM flag or GPU timeout. Targeted POSIX_FADV_DONTNEED on the model files released about58GiB of cached pages; subsequent starts required100GiB free and succeeded without swap growth. **Warm-cache startup remains an unresolved qualification issue.** Model contents and compilation cache were retained.
- 001500: stopped after966.7seconds on JSONDecodeError. The actual controller writer truncated status files in place; earlier descriptions calling its output atomic were wrong. All controller snapshots now use a temporary file, fsync, and atomic replacement. The actual old function reproducibly exposed emptyJSON during a write; the new function preserved the previous complete snapshot. Client failure records now include tracebacks.

Eleven CPU/fake-HTTP regressions pass locally and on blackbox. They cover cancellation, incomplete SSE, total deadlines, loss of coverage, tokenizer payloads/calibration, stale/rebooted/missing-worker rejection, clock/read races, primary-versus-summary timestamps, and atomic/interrupted publication. These checks validate the harness, not GPU reliability.

The final planned idle observation period was not reached. The test does not qualify maximum concurrent execution, heavy graphics/thermal load, arbitrary schedules/seeds, long-context answer accuracy, or the previously failing70tok/s performance target. A further full-hour attempt should first isolate the independent recorder's I/O bottleneck and resolve startup memory pressure. The failed attempts are retained separately, not combined into a pass.

Evidence: `REVIEWED-TRANSITION-RESULT.json`, campaign `attribution-20260911T004000Z` on blackbox/workstation, and lossless local archives. Source: workstation `/var/home/dylan/projects/inference/r9v/diagnostics/gpu-attribution/{transition_stress.py,atomic_json.py,test_transition_stress.py,TRANSITION-STRESS.md}`. No commit, push, or publication was performed.
